import React from "react";
import QuickLinks from "./QuickLinks";

const QuickLinksContainer = ({ linksArray }) => {
  return <QuickLinks links={linksArray} />;
};

export default QuickLinksContainer;
